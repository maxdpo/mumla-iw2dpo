--- a/app/build.gradle
+++ b/app/build.gradle
@@ -78,7 +78,7 @@
         // Custom test build for IW2DPO's PMR/POC group: adds a "roger beep" transmitted on
         // PTT release, plus automatic connect/reconnect to the last used server. Based on
         // upstream Mumla (gitDescribe below), not an official release.
-        versionName gitDescribe + "-IW2DPO-autoconnect-beta4"
+        versionName gitDescribe + "-IW2DPO-autoconnect-beta5"
 
         buildConfigField "long", "TIMESTAMP", System.currentTimeMillis() + "L"
         buildConfigField "String", "VERSIONTAG", "\"" + gitDescribe.split("-")[0] + "\""
